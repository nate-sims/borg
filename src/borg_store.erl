-module(borg_store).

%% Module Functions
-export([
	 init/0
	 ]).

%% Store Functions
-export([
	 add/1,
	 delete/1,
	 get/1,
	 update/2
	 ]).


init() ->
    ok.

add(_Node) ->
   ok.

delete(_Node) -> 
    ok.

get(_Node) ->
    ok.

update(_Node, _Attrs) -> 
    ok.

